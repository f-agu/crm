{% extends 'base.html.twig' %}

{% block title %}C&eacute;nacle R&eacute;mi Mollet{% endblock %}


{% block body %}

	<nav id="sidebar">
		<div class="sidebar-header">
			<a class="col-sm-3 col-md-2 mr-0" style="padding:2px!important" href="{{ path('home') }}"><img src="/assets/img/logo.png" style="max-height:70px" /></a>
		</div>
		<ul class="list-unstyled components">
			{{ render(controller('App\\Controller\\MenuLeftController::viewMenuLeft')) }}
		</ul>
	</nav>
	        
	{#


	<nav class="navbar navbar-dark fixed-top bg-dark flex-md-nowrap p-0">
		<a class="col-sm-3 col-md-2 mr-0" style="padding:2px!important" href="{{ path('home') }}"><img src="/assets/img/logo.png" style="max-height:32px" /></a>
		<input class="form-control form-control-dark w-100" type="text" placeholder="Rechercher" aria-label="Rechercher">
		<ul class="navbar-nav px-3">
			<li class="nav-item text-nowrap">
				{% if user == null %}
					<h3><a class="nav-link" href="/login">{% trans %}Sign in{% endtrans %}</a></h3>
				{% else %}
					<h3><a class="nav-link" href="/logout">{% trans %}Sign out{% endtrans %}</a></h3>
				{% endif %}
			</li>
		</ul>
	</nav>
	 
	    <nav class="navbar navbar-light bg-light navbar-expand-lg"> 
	        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
	        	<span class="navbar-toggler-icon"></span>
	        </button>
	        <!-- appeler le logo apres le bouton -->
	        <a class="navbar-brand" href="#">Logo</a>
	        
	        <!-- suite du menu -->
	    </nav>
	  #}  

	<div id="content">
		<nav class="navbar navbar-expand-lg navbar-light bg-light">
			<div class="container-fluid">
					<button type="button" id="sidebarCollapse" class="btn btn-info">
					<i class="fas fa-align-left"></i>
					<span data-feather="menu"></span>
				</button>
				<input class="form-control form-control-dark" type="text" placeholder="Rechercher" aria-label="Rechercher">
				<ul class="navbar-nav px-3">
					<li class="nav-item text-nowrap">
						{% if connectedUser is defined and connectedUser != null %}
							<h5><a class="nav-link" href="/logout">{% trans %}Sign out{% endtrans %}</a></h5>
						{% else %}
							<h5><a class="nav-link" href="/login">{% trans %}Sign in{% endtrans %}</a></h5>
						{% endif %}
					</li>
				</ul>
			</div>
		</nav>

		<div>
			{% block dashboardcontent %}{% endblock %}
		</div>


	</div>

{% endblock %}


{% block javascripts %}
	<script src="/vendor/jquery/jquery-3.3.1.min.js"></script>
	<script src="/vendor/popper/popper.min.js"></script>
	<script src="/vendor/bootstrap/bootstrap.min.js"></script>
	<script src="/vendor/malihu-custom-scrollbar-plugin/jquery.mCustomScrollbar.concat.min.js"></script>
	<script src="/vendor/feather/feather.min.js"></script>
	<script type="text/javascript">
		feather.replace();
		$(document).ready(function () {
			$("#sidebar").mCustomScrollbar({
				theme: "minimal"
			});

		$('#sidebarCollapse').on('click', function () {
			$('#sidebar, #content').toggleClass('active');
				$('.collapse.in').toggleClass('in');
				$('a[aria-expanded=true]').attr('aria-expanded', 'false');
			});
		});
	</script>
	<script src="/vendor/jquery.serializeobject/jquery.serializeObject.min.js"></script>
{% endblock %}

